@model AWESOME.Models.ViewModels.DespachosIndexViewModel

@{
    ViewData["Title"] = "Despachos - Control Tower";
}

<div class="container-fluid awesome-despachos">

    <!-- HEADER KPI -->
    <div class="row g-3 mb-4">

        <!-- TOTAL DEL DÍA -->
        <div class="col-md-4">

            <div class="card border-0 shadow-sm">

                <div class="card-body">

                    <div class="d-flex justify-content-between align-items-center">

                        <div>
                            <h6 class="text-muted mb-1">Despachos del día</h6>
                            <h3 class="mb-0 fw-bold">
                                @Model.TotalHoy
                            </h3>
                        </div>

                        <div class="icon-box bg-danger-subtle text-danger">
                            <i class="bi bi-arrow-up-circle"></i>
                        </div>

                    </div>

                </div>

            </div>

        </div>

        <!-- FECHA -->
        <div class="col-md-8">

            <div class="card border-0 shadow-sm">

                <div class="card-body">

                    <form method="get" class="d-flex align-items-end gap-3 flex-wrap">

                        <div>

                            <label class="form-label small text-muted">
                                Fecha de consulta
                            </label>

                            <input type="date"
                                   name="fecha"
                                   value="@Model.FechaSeleccionada.ToString("yyyy-MM-dd")"
                                   class="form-control" />

                        </div>

                        <a asp-action="Index" class="btn btn-secondary">
                            Hoy
                        </a>

                        <div class="col-md-4"> 
                            <label class="form-label">Contenedor</label> 
                            <input type="text" 
                                name="contenedor" 
                                value="@Model.Contenedor" 
                                class="form-control" 
                                placeholder="Buscar contenedor..." /> 
                            </div>

                        <div class="col-md-4"> 
                            <label class="form-label">Referencia</label> 
                            <input type="text" 
                                name="contenedor" 
                                value="@Model.ContenedorReferencia" 
                                class="form-control" 
                                placeholder="Buscar contenedor..." /> 
                            </div>

                        <div>

                            <button class="btn btn-primary">
                                <i class="bi bi-search"></i>
                                Consultar
                            </button>

                        </div>

                        <div class="text-muted small ms-auto">
                            <i class="bi bi-clock"></i>
                            Última actualización: @DateTime.Now.ToString("HH:mm")
                        </div>

                    </form>

                </div>

            </div>

        </div>

    </div>

    <!-- TABLA CONTROL TOWER -->
    <div class="card border-0 shadow-sm">

        <div class="card-header bg-white d-flex justify-content-between align-items-center">

            <div>
                <h6 class="mb-0 fw-semibold">
                    Lista de contenedores despachados
                </h6>
                <small class="text-muted">
                    Vista operativa en tiempo real desde SCL
                </small>
            </div>

            <span class="badge bg-dark">
                @Model.Despachos.Count registros
            </span>

        </div>

        <div class="card-body p-0">

            <div class="table-responsive">

                <table class="table table-hover align-middle mb-0">

                    <thead class="table-light">

                        <tr>
                            <th>Hora</th>
                            <th>Contenedor</th>
                            <th>Marchamos</th>
                            <th>Cliente</th>
                            <th>Referencia</th>
                        </tr>

                    </thead>

                    <tbody>

                        @foreach (var item in Model.Despachos)
                        {
                            <tr>

                                <td>
                                    @item.FechaHoraDespacho.ToString("HH:mm")
                                </td>

                                <td class="fw-bold">
                                    @item.Contenedor
                                </td>

                                <td>
                                    @item.Marchamos
                                </td>

                                <td>
                                    @item.Informacion
                                </td>

                                <td>
                                    @item.ContenedorReferencia
                                </td>

                            </tr>
                        }

                    </tbody>

                </table>

            </div>

        </div>

    </div>

</div>

<style>
    .awesome-despachos .icon-box {
        width: 48px;
        height: 48px;
        border-radius: 12px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 20px;
    }

    .awesome-despachos table tbody tr:hover {
        background: #f8fafc;
    }

    .awesome-despachos table td {
        font-size: 14px;
        white-space: nowrap;
    }

    .awesome-despachos .table-responsive {
        max-height: 65vh;
        overflow-y: auto;
    }
</style>